build:
  python_version: "3.10"
  python_packages:
    - -r requirements.txt

predict: 
  predictor: predict.py:Predictor
  inputs:
    pdf_file:
      type: path
      description: "Upload a single PDF file"
